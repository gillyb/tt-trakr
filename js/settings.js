
var settingsService = angular.module('SettingsService', []);

settingsService.factory('Settings', ['$http', '$q', function($http, $q) {

	var fs = require('fs');

	var settings = {
		
	};

	return settings;

}]);